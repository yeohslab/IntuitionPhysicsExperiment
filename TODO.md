# 心理物理学实验控制与渲染规范：摆球与弹簧振子单元

## 注

以平衡位置向右为正, 向左为负

## 1. 摆球练习单元

**功能**：在屏幕上精确显示一个摆球在重力作用下的非线性动力学运动。**注：显示要准！** 摆杆的旋转角度与摆球的几何位置需实时物理对齐，必须精确体现大角度时的“变慢”效应或绕圈时的速度变化, 一开始只显示真实情境(即不要显示大圆周)

**字段定义**：

* **初始角度 $\theta_0$**（角度制，默认：$45^\circ$；转换为弧度参与计算，以向下垂直方向为 $0$，顺时针为正）
* **初始角速度 $\omega_0$**（角度制/秒，默认：$0^\circ/\text{s}$）
* **杆长 $l$**（默认：$4.0 \text{ m}$）
* **重力加速度 $g$**（默认：$9.8 \text{ m/s}^2$）
* **显示时间**（默认：$4T$）
* **能量 $E$**（不可自定义, 以最低点为势能零点，根据参数精确计算）：

$$E = \frac{1}{2}m(l\omega_0)^2 + mgl(1 - \cos\theta_0)$$


* **周期 $T$（精确计算，必须区分以下两种动力学状态，严禁小角度近似）**：
设定临界能量为 $E_{\text{crit}} = 2mgl$（即刚好到达最高点 $\theta = \pi$ 且速度为 $0$ 的临界状态）。
* **情况一：往复摆动状态 ($E < E_{\text{crit}}$)**
最大摆角 $\theta_{\max} = \arccos\left(1 - \frac{E}{mgl}\right)$。令模数 $k = \sin\left(\frac{\theta_{\max}}{2}\right)$。

$$T = 4\sqrt{\frac{l}{g}} K(k)$$



其中 $K(k) = \int_0^{\pi/2} \frac{d\phi}{\sqrt{1 - k^2\sin^2\phi}}$ 为第一类完全椭圆积分。
* **情况二：越过最高点绕圈状态 ($E > E_{\text{crit}}$)**
摆球向一个方向无限旋转。令模数 $k = \sqrt{\frac{2mgl}{E}}$。

$$T = 2\sqrt{\frac{l}{g}} k K(k)$$



*(此时周期 $T$ 严格定义为摆球连续两次通过最低点，即转动一周 $2\pi$ 所需的时间)*


* **运动方程（供程序渲染使用）**：
* 在往复摆动状态下，任意时刻角度由雅可比椭圆函数（Jacobi Elliptic Functions）给出精确解：

$$\theta(t) = 2 \arcsin\left[ k \cdot \text{sn}\left( \sqrt{\frac{g}{l}}t + \psi, k \right) \right]$$



其中 $\psi$ 为由初始条件 $(\theta_0, \omega_0)$ 决定的初相。



---

## 2. 摆球刺激单元

**功能**：在屏幕上显示摆球的运动。在指定的“显示时间”内可见，在“隐藏时间”内隐形。摆球在隐形状态下仍在后台进行精确的非线性物理时历推算。最后，要求受试者汇报对摆球最终角位置（角度）的估计。

**字段定义**：

* **初始角度 $\theta_0$**（角度制，默认：$45^\circ$）
* **初始角速度 $\omega_0$**（角度制/秒，默认：$0^\circ/\text{s}$）
* **杆长 $l$**（默认：$4.0 \text{ m}$）
* **重力加速度 $g$**（默认：$9.8 \text{ m/s}^2$）
* **总显示时间**（默认：$4T$）
* **第一显示时间**（默认：$1.9T$）
* **第一隐藏时间**（默认：$1.7T$）
* **第二显示时间**（默认：$1.3T$）
* **第二隐藏时间**（默认：$1.1T$）
*(注：总时历为各显示与隐藏时间的总和，或总时间截止时触发汇报机制)*
* **能量 $E$**（根据参数精确计算，同单元 1）
* **周期 $T$**（根据单元 1 中的椭圆积分公式精确计算，自动识别并匹配“摆动”或“绕圈”情况）
* **估计目标**：系统在总时历结束时立刻冻结画面，清除滑块图像, 显示圆周，当受试者通过鼠标点击界面时, 在对应位置重新显示杆摆，然后通过拖动精调, 标出其“感知中”的摆杆最终角度 $\theta_{\text{estimated}}$。系统计算并汇报**角度偏差量 $\Delta \theta = \theta_{\text{estimated}} - \theta_{\text{actual}}$**(不显示给被试, 作为导出数据)。

---

## 3. 弹簧振子练习单元

**功能**：在屏幕上精确显示一个横向弹簧振子（滑块与弹簧）的动力学运动(左边固定, 中间弹簧, 右边滑块)。**注：显示要准！** 滑块位移与弹簧形变需实时物理对齐，确保弹簧视觉拉伸与压缩的均匀性。一开始只显示真实情境(即不要显示坐标原点, 坐标系)

**字段定义**：

* **滑块质量 $m$**（默认：$1.0 \text{ kg}$）
* **弹簧劲度系数 $k$**（默认：$4.0 \text{ N/m}$）
* **初始位移 $x_0$**（米，默认：$0.5 \text{ m}$，以平衡位置为原点，向右为正）
* **初始速度 $v_0$**（米/秒，默认：$0 \text{ m/s}$）
* **显示时间**（默认：$4T$）
* **能量 $E$**（根据参数精确计算）：

$$E = \frac{1}{2}kx_0^2 + \frac{1}{2}mv_0^2$$


* **周期 $T$（精确计算）**：
由于横向理想弹簧振子在弹性限度内是完美的线性回复力（$F = -kx$），其周期独立于振幅：

$$T = 2\pi \sqrt{\frac{m}{k}}$$


* **运动方程（供程序渲染使用）**：
振幅 $A = \sqrt{x_0^2 + (v_0/\omega)^2}$，初相 $\phi = \text{arctan2}(-\frac{v_0}{\omega}, x_0)$，其中系统角频率 $\omega = \sqrt{\frac{k}{m}}$。
任意时刻滑块相对于平衡位置的位移：

$$x(t) = A \cos(\omega t + \phi)$$



> **💡 动力学边界注意**：
> 本单元默认采用理想线性弹簧模型。

---

## 4. 弹簧振子刺激单元

**功能**：在屏幕上显示横向弹簧振子的运动(左边固定, 中间弹簧, 右边滑块)。在指定的“显示时间”内可见，在“隐藏时间”内隐形。滑块在隐形状态下仍在后台进行精确的物理时历推算。最后，要求受试者汇报（或系统评估）对滑块最终位置/位移的估计。

**字段定义**：

* **滑块质量 $m$**（默认：$1.0 \text{ kg}$）
* **弹簧劲度系数 $k$**（默认：$4.0 \text{ N/m}$）
* **初始位移 $x_0$**（米，默认：$0.5 \text{ m}$）
* **初始速度 $v_0$**（米/秒，默认：$0 \text{ m/s}$）
* **总显示时间**（默认：$4T$）
* **第一显示时间**（默认：$1.9T$）
* **第一隐藏时间**（默认：$1.7T$）
* **第二显示时间**（默认：$1.3T$）
* **第二隐藏时间**（默认：$1.1T$）
*(注：总时历为各显示与隐藏时间的总和，或总时间截止时触发汇报机制)*
* **能量 $E$**（根据参数精确计算，同单元 3）
* **周期 $T$**（精确计算，$T = 2\pi \sqrt{\frac{m}{k}}$）

* **估计目标**：系统在总时历结束时冻结画面，清除滑块图像，显示运动直线轨迹, 当受试者通过鼠标点击界面时, 在对应位置重新显示滑块，然后通过拖动精调,标出其“感知中”的滑块最终位置。系统计算并汇报**位置偏差量 $\Delta x = x_{\text{estimated}} - x_{\text{actual}}$**(不显示给被试, 作为导出数据)。
---

## 🛠 视觉渲染与系统底层规范（确保“显示要准”）

### 1. 坐标系与几何映射

* **单摆单元**：必须明确标记出**旋转中心（悬挂点）**，并将其设为局部坐标系原点 $(0,0)$。向下垂直方向为角度 $\theta = 0$ 的基准线。屏幕像素坐标 $(X, Y)$ 映射公式：

$$X = X_{\text{anchor}} + L_{\text{pixel}} \cdot \sin\theta(t)$$


$$Y = Y_{\text{anchor}} + L_{\text{pixel}} \cdot \cos\theta(t)$$



*(注：该公式完美契合向右为 $X+$、向下为 $Y+$ 的标准屏幕坐标系)*
* **弹簧振子单元**：不明确标记出**平衡位置（零点）**。弹簧的左端固定在屏幕固定锚点，右端连接滑块质心。

### 2. 动效细节对齐

* **弹簧绘制**：严禁将弹簧图片做整体的二维拉伸失真处理。必须通过代码实时绘制锯齿波（Zigzag）或正弦曲线，根据实时位移 $x(t)$ 动态调整波节距（Pitch），呈现真实的物理压缩与拉伸视觉。
* **摆动非线性**：在大角度单摆实验中，渲染帧率必须足以体现摆球在最高点减速、最低点加速的非线性视觉流（Visual Flow），避免肉眼可见的卡顿。

### 3. 时间同步与积分器安全（防止后台漂移）

* 由于涉及多段“显示/隐藏”交替控制，当滑块或摆球在后台隐形时，物理引擎**绝不能停滞**。
* 为了防止在隐形期间由于累积误差导致能量不守恒（例如摆幅越摆越大，或弹簧速度失真），若不直接使用解析解（雅可比椭圆函数/三角函数），程序必须采用辛积分器（如 Symplectic Euler 或 Velocity Verlet）进行物理递推，并由高精度的固定时间步长（Fixed Timestep）驱动，确保隐形前后的物理数据绝对连贯、准确。